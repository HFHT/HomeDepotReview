# Habitat Tucson Receipt Review

## Todo
Convert the receipt edit pages to useForm, only update store when the entire field is changed and not on each key stroke.

Add error validation to the fields. 

Do I need to normalize the auditTrail data types (currency, number, date, string) currently they are all string. 

Header, click on logo changes route to / 
Header, bell icon to show how many incomplete entries there are.